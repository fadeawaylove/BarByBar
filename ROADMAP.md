# BarByBar 长期路线图

## 产品方向

BarByBar 是一个本地优先的期货逐根训练与交易复盘系统。核心闭环是：

1. 准备训练：选择数据、起点、规则和目标。
2. 隐藏未来逐根决策：计划、执行和管理交易。
3. 结构化复盘：区分交易结果与执行质量。
4. 长期洞察：发现重复出现的优势、错误和下一次训练目标。

不直接改善以上任一环节的新功能，默认进入候选池，不插入当前里程碑。

## 版本路线

| 版本 | 主题 | 目标状态 |
| --- | --- | --- |
| v0.6 | 基础收口 | 更快、更稳、数据更安全、现有界面更一致 |
| v0.7 | 训练流程 | 建立准备、训练、完成总结的完整流程 |
| v0.8 | 专业复盘 | 建立结构化交易评价和纪律评分 |
| v0.9 | 统计洞察 | 支持跨案例比较、趋势和错误分析 |
| v1.0 | 正式产品化 | 完成新手引导、迁移、恢复、文档和发布质量门槛 |
| v1.x | 能力扩展 | 暗色主题、成交量、指标、自定义模板和可选同步 |

## 当前里程碑

当前执行：`v0.6 基础收口`

执行入口：`openspec/changes/deliver-v0-6-foundation/tasks.md`

v0.6 的范围：

- 收口逐根推进、图表交互和大案例性能。
- 增加本地数据库备份、恢复和交易数据导出能力。
- 增加 CSV 导入预览、字段确认和数据质量反馈。
- 完成工作台基础体验修正，包括术语、默认视窗和状态反馈。
- 固化性能、测试、截图、安装包和发布验证门槛。

## 长期任务恢复协议

每次开始工作时，按以下顺序恢复上下文：

1. 阅读本文件，确认当前里程碑和明确不做的范围。
2. 运行 `openspec status --change <current-change>`。
3. 阅读当前 change 的 `proposal.md`、`design.md` 和 `tasks.md`。
4. 查看 `git status` 和最近提交，识别未提交或已完成的工作。
5. 从 `tasks.md` 中第一个未完成且依赖满足的任务继续。
6. 修改前先运行对应的基线测试或基准，保留可比较结果。

每次结束工作时，必须留下以下检查点：

1. 更新 `tasks.md` 的完成状态，不把部分完成标记为完成。
2. 记录重要验证结果、遗留问题和下一项任务。
3. 运行与改动风险匹配的测试、性能检查或截图检查。
4. 保持提交主题单一，并使用可理解的中文提交说明。
5. 稳定检查点提交后自动推送到远程仓库；稳定检查点必须满足任务状态已更新、相关验证通过、工作区不存在误带改动。
6. 如果推送失败，保留本地提交并在交接记录中明确失败原因，下一次恢复工作时优先补推。
7. 需要发布时使用补丁版本发布，并确认 Release 产物真实存在；普通检查点推送不等同于发布新版本。

换新对话时，只需说明“继续 BarByBar 路线图当前里程碑”；执行者应按以上协议从仓库恢复，而不依赖聊天记录。

## 迭代规则

- 一个版本只围绕一个主要主题推进。
- 高频训练路径优先于低频管理界面。
- 先采集可靠、结构化的数据，再开发统计图表。
- 数据库结构变化必须包含迁移、自动备份和回退验证。
- 性能优化必须保留优化前后的基准结果。
- 界面改动必须检查空状态、加载状态、错误状态和窄屏状态。
- 行为测试、视觉检查和安装包检查共同构成完成标准。
- 补丁版本处理缺陷和小范围完善；小版本交付完整能力。

## 候选池

以下方向不进入 v0.6：

- 专业暗色主题。
- 成交量副图和更多技术指标。
- 自动播放。
- 云同步、账号和社区系统。
- 多图表联动和插件系统。
